---
import Layout from '../layouts/Layout.astro';
import { unidades, preguntasPorUnidad } from '../data';

const unidadesConPreguntas = unidades.map(unidad => ({
  ...unidad,
  numPreguntas: preguntasPorUnidad[unidad.id as keyof typeof preguntasPorUnidad]?.length || 0
}));

const totalPreguntas = unidadesConPreguntas.reduce((acc, u) => acc + u.numPreguntas, 0);
const totalUnidades = unidades.length;
---

<Layout title="Flutter Quiz - Test Interactivo de Flutter">
  <!-- Hero Section con efecto visual mejorado -->
  <section class="text-center mb-16 relative">
    <!-- Elementos decorativos de fondo -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div class="absolute top-20 left-10 w-72 h-72 bg-cyan-500/20 rounded-full blur-3xl"></div>
      <div class="absolute top-40 right-10 w-96 h-96 bg-purple-500/20 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[600px] h-64 bg-blue-500/10 rounded-full blur-3xl"></div>
    </div>
    
    <div class="max-w-4xl mx-auto relative">
      <div class="animate-float mb-6">
        <span class="text-8xl drop-shadow-2xl">🦋</span>
      </div>
      
      <h1 class="text-5xl md:text-7xl font-extrabold mb-6 tracking-tight">
        <span class="text-shimmer">Flutter Quiz</span>
      </h1>
      
      <p class="text-2xl md:text-3xl text-slate-200 mb-4 font-medium">
        Test Interactivo para el Curso de Flutter
      </p>
      
      <p class="text-lg text-slate-400 mb-10 max-w-2xl mx-auto">
        Domina Flutter y Dart con <span class="text-white font-bold text-xl">{totalPreguntas}</span> preguntas 
        cuidadosamente diseñadas en <span class="text-white font-bold text-xl">{totalUnidades} unidades</span> temáticas
      </p>
      
      <!-- Badges animados -->
      <div class="flex flex-wrap justify-center gap-3 mb-10">
        <div class="px-5 py-2.5 bg-gradient-to-r from-cyan-400 to-cyan-600 rounded-full flex items-center gap-2 shadow-lg shadow-cyan-500/25 transform hover:scale-105 transition-transform">
          <span class="text-xl">🎯</span>
          <span class="font-semibold">Dart</span>
        </div>
        <div class="px-5 py-2.5 bg-gradient-to-r from-blue-400 to-blue-600 rounded-full flex items-center gap-2 shadow-lg shadow-blue-500/25 transform hover:scale-105 transition-transform">
          <span class="text-xl">🧩</span>
          <span class="font-semibold">Widgets</span>
        </div>
        <div class="px-5 py-2.5 bg-gradient-to-r from-purple-400 to-purple-600 rounded-full flex items-center gap-2 shadow-lg shadow-purple-500/25 transform hover:scale-105 transition-transform">
          <span class="text-xl">🧭</span>
          <span class="font-semibold">Navegación</span>
        </div>
        <div class="px-5 py-2.5 bg-gradient-to-r from-orange-400 to-orange-600 rounded-full flex items-center gap-2 shadow-lg shadow-orange-500/25 transform hover:scale-105 transition-transform">
          <span class="text-xl">🔥</span>
          <span class="font-semibold">Firebase</span>
        </div>
      </div>

      <!-- CTA principal -->
      <a 
        href="#unidades" 
        class="inline-flex items-center gap-3 px-8 py-4 bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 rounded-2xl font-bold text-lg shadow-xl shadow-purple-500/30 hover:shadow-purple-500/50 transform hover:scale-105 transition-all"
      >
        <span>Comenzar Ahora</span>
        <svg class="w-5 h-5 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div>
  </section>

  <!-- Cards de Unidades -->
  <section id="unidades" class="scroll-mt-8">
    <div class="text-center mb-10">
      <h2 class="text-3xl md:text-4xl font-bold mb-3">
        <span class="bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          Elige tu Unidad
        </span>
      </h2>
      <p class="text-slate-400 text-lg">Cada unidad contiene preguntas específicas del temario</p>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {unidadesConPreguntas.map((unidad, index) => (
        <a 
          href={`/quiz/${unidad.id}`}
          class="card-glow group relative bg-gradient-to-br from-slate-800/80 to-slate-900/80 backdrop-blur-xl rounded-3xl p-7 border border-slate-700/50 hover:border-slate-500/50 transition-all duration-500 hover:transform hover:scale-[1.03] hover:-translate-y-2 overflow-hidden"
        >
          <!-- Gradient overlay animado -->
          <div class={`absolute inset-0 bg-gradient-to-br ${unidad.color} opacity-0 group-hover:opacity-15 transition-all duration-500`}></div>
          
          <!-- Icono grande de fondo -->
          <div class="absolute -right-4 -top-4 text-8xl opacity-10 group-hover:opacity-20 transition-opacity">
            {unidad.icono}
          </div>
          
          <!-- Header -->
          <div class="relative mb-5">
            <span class="inline-block px-3 py-1 bg-slate-700/50 rounded-lg text-xs text-slate-400 uppercase tracking-wider font-medium mb-3">
              Unidad {index + 3}
            </span>
            <div class="flex items-center gap-3">
              <span class="text-4xl filter drop-shadow-lg">{unidad.icono}</span>
              <h3 class={`text-2xl font-bold bg-gradient-to-r ${unidad.color} bg-clip-text text-transparent`}>
                {unidad.titulo}
              </h3>
            </div>
          </div>

          <!-- Description -->
          <p class="text-slate-300 text-sm mb-5 relative leading-relaxed">
            {unidad.descripcion}
          </p>

          <!-- Tags mejorados -->
          <div class="flex flex-wrap gap-2 mb-5 relative">
            {unidad.temas.slice(0, 4).map(tema => (
              <span class="px-3 py-1.5 bg-slate-700/70 hover:bg-slate-600/70 rounded-xl text-xs text-slate-200 font-medium transition-colors">
                {tema}
              </span>
            ))}
          </div>

          <!-- Footer mejorado -->
          <div class="flex items-center justify-between relative pt-4 border-t border-slate-700/50">
            <div class="flex items-center gap-2">
              <div class={`w-3 h-3 rounded-full bg-gradient-to-r ${unidad.color}`}></div>
              <span class="text-sm text-slate-400 font-medium">
                {unidad.numPreguntas} preguntas
              </span>
            </div>
            <div class={`flex items-center gap-2 text-sm font-bold bg-gradient-to-r ${unidad.color} bg-clip-text text-transparent`}>
              <span>Iniciar</span>
              <svg class="w-5 h-5 group-hover:translate-x-2 transition-transform text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </div>
          </div>
        </a>
      ))}
    </div>
  </section>

  <!-- Sección de características mejorada -->
  <section class="mt-20">
    <div class="bg-gradient-to-br from-slate-800/50 to-slate-900/50 backdrop-blur-xl rounded-3xl p-10 border border-slate-700/50">
      <h2 class="text-2xl md:text-3xl font-bold mb-10 text-center">
        <span class="bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          ¿Por qué usar Flutter Quiz?
        </span>
      </h2>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        <div class="text-center p-6 bg-slate-800/50 rounded-2xl border border-slate-700/50 hover:border-slate-600/50 transition-all hover:transform hover:scale-105">
          <span class="text-5xl mb-4 block">🎲</span>
          <h3 class="font-bold text-lg mb-2 text-white">Aleatorio</h3>
          <p class="text-sm text-slate-400 leading-relaxed">Las preguntas se mezclan en cada intento para un aprendizaje efectivo</p>
        </div>
        <div class="text-center p-6 bg-slate-800/50 rounded-2xl border border-slate-700/50 hover:border-slate-600/50 transition-all hover:transform hover:scale-105">
          <span class="text-5xl mb-4 block">💡</span>
          <h3 class="font-bold text-lg mb-2 text-white">Explicaciones</h3>
          <p class="text-sm text-slate-400 leading-relaxed">Cada respuesta incluye una explicación detallada del concepto</p>
        </div>
        <div class="text-center p-6 bg-slate-800/50 rounded-2xl border border-slate-700/50 hover:border-slate-600/50 transition-all hover:transform hover:scale-105">
          <span class="text-5xl mb-4 block">📊</span>
          <h3 class="font-bold text-lg mb-2 text-white">Estadísticas</h3>
          <p class="text-sm text-slate-400 leading-relaxed">Visualiza tu progreso por categoría y tema</p>
        </div>
        <div class="text-center p-6 bg-slate-800/50 rounded-2xl border border-slate-700/50 hover:border-slate-600/50 transition-all hover:transform hover:scale-105">
          <span class="text-5xl mb-4 block">🔄</span>
          <h3 class="font-bold text-lg mb-2 text-white">Ilimitado</h3>
          <p class="text-sm text-slate-400 leading-relaxed">Practica todas las veces que necesites sin restricciones</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="mt-16 mb-8">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <div class="text-center p-6 bg-slate-800/30 rounded-2xl border border-slate-700/30">
        <span class="text-3xl mb-2 block">❓</span>
        <p class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          {totalPreguntas}
        </p>
        <p class="text-slate-400 text-sm mt-1">Preguntas</p>
      </div>
      <div class="text-center p-6 bg-slate-800/30 rounded-2xl border border-slate-700/30">
        <span class="text-3xl mb-2 block">📚</span>
        <p class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          {totalUnidades}
        </p>
        <p class="text-slate-400 text-sm mt-1">Unidades</p>
      </div>
      <div class="text-center p-6 bg-slate-800/30 rounded-2xl border border-slate-700/30">
        <span class="text-3xl mb-2 block">✨</span>
        <p class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          100%
        </p>
        <p class="text-slate-400 text-sm mt-1">Gratuito</p>
      </div>
      <div class="text-center p-6 bg-slate-800/30 rounded-2xl border border-slate-700/30">
        <span class="text-3xl mb-2 block">🔁</span>
        <p class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">
          ∞
        </p>
        <p class="text-slate-400 text-sm mt-1">Intentos</p>
      </div>
    </div>
  </section>
</Layout>
